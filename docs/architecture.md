# Architecture Notes

## Current Scope

This foundation includes React Router, a shared layout, global styles, and minimal placeholder components.

## Future Scope

- Install and validate Tailwind CSS.
- Add content models in `src/data`.
- Implement the interactive India map.
- Add Framer Motion only when animation requirements are known.
- Add Lucide React only when the navigation and content actions are designed.
